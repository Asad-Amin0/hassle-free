import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const _geist = Geist({ subsets: ["latin"] });
const _geistMono = Geist_Mono({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: 'HASSLE-FREE | AI-Powered Career Enhancement & Recruitment Platform',
  description: 'Transform your job search with AI-powered resume analysis, mock interviews, and intelligent job matching. Get hired faster with personalized career guidance.',
  generator: 'HASSLE-FREE',
  keywords: ['AI recruitment', 'resume analysis', 'job matching', 'mock interviews', 'career enhancement', 'employability score'],
  authors: [{ name: 'F25CS124 - University of Central Punjab' }],
  icons: {
    icon: [
      {
        url: '/icon-light-32x32.png',
        media: '(prefers-color-scheme: light)',
      },
      {
        url: '/icon-dark-32x32.png',
        media: '(prefers-color-scheme: dark)',
      },
      {
        url: '/icon.svg',
        type: 'image/svg+xml',
      },
    ],
    apple: '/apple-icon.png',
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className="font-sans antialiased bg-background">
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
